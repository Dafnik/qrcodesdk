---
'@qrcodesdk/cli': patch
'@qrcodesdk/core': patch
---

Generate smaller QR codes for mixed input by automatically splitting data into optimized numeric, alphanumeric, and byte segments. Add optional UTF-8 ECI encoding through the core options, builder API, and the CLI's `--eci` flag.
