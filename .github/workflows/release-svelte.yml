name: release @qrcodesdk/svelte

on:
  push:
    tags:
      - '@qrcodesdk/svelte@**'

permissions:
  contents: read

jobs:
  svelte:
    permissions:
      contents: write
      id-token: write
    uses: ./.github/workflows/release.yml
    with:
      lib: svelte
