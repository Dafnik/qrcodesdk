# Allow temporary framework parity gaps

Core and Browser capabilities may ship without framework wrappers, and a framework package may ship
a new applicable capability before the other framework packages. QRCodeSDK still aims for eventual
parity across React, Vue, Svelte, and Angular where their models permit it, but parity does not block
individual package releases and framework constraints may produce documented differences.
