# Version public packages independently

Each public QRCodeSDK package has its own version and may release without publishing unchanged
packages. This matches independent Core, Browser, Node, CLI, and framework delivery, while making
declared peer-dependency ranges and cross-package compatibility documentation part of the release
discipline.
