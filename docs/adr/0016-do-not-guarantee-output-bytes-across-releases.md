# Do not guarantee generated bytes across releases

Identical payload and options produce deterministic output for a given QRCodeSDK implementation, but
matrix modules, SVG markup, and PNG bytes are not compatibility contracts across releases. Encoding,
mask selection, markup, and rasterization may improve while preserving the payload and documented
option behavior; consumers that require exact artifacts must pin the package version and keep their
own fixtures.
