# Use framework-specific support windows

QRCodeSDK follows each framework ecosystem rather than imposing one version rule: Angular supports
all upstream-supported majors, React supports explicitly selected widely used stable majors, Vue
supports the current major from a tested minimum minor, and Svelte supports the current major. React
support is reviewed manually because React publishes no Angular-style LTS window.
