# Define 1.0 as contract stability rather than feature completeness

A package reaches 1.0 when its existing public contracts are correct and maintainable under semantic
versioning, not when no desirable features remain. New payload helpers, renderers, styling options,
and other additive capabilities may ship in later minor releases, so an open-ended feature backlog
does not delay stabilization.
