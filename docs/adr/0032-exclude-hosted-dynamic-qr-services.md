# Exclude hosted dynamic QR services

QRCodeSDK does not provide hosted redirects, destination management, user accounts, scan analytics,
or other services commonly sold as "dynamic QR codes." The SDK encodes the payload supplied by the
developer; changing a redirect destination without regenerating the symbol belongs to an external
service.
