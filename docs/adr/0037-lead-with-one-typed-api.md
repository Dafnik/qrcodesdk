# Lead with one typed API across outputs and frameworks

QRCodeSDK's primary product claim is one typed API across matrix generation, output formats, and
framework components. Runtime isolation, ESM-only packaging, shared styling, and center images
support that claim but do not replace it as the headline reason to choose the SDK.
