{
  "name": "@repo/core-testing",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "types": "./dist/index.d.mts",
  "exports": {
    ".": "./dist/index.mjs",
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown",
    "watch": "tsdown --watch",
    "test": "vitest --watch false --exclude 'tests/svg/*roundtrip.e2e.spec.ts'",
    "test:coverage:raw": "vitest --coverage --mode coverage --watch false",
    "test:roundtrips": "vitest --watch false tests/svg/*roundtrip.e2e.spec.ts",
    "check-types": "tsc --noEmit && tsc -p tsconfig.spec.json",
    "lint": "eslint scripts src tests --max-warnings 0",
    "clean": "rm -rf .turbo dist"
  },
  "dependencies": {
    "@qrcodesdk/core": "workspace:*"
  },
  "devDependencies": {
    "@types/qrcode": "catalog:",
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "qrcode": "catalog:",
    "qrcode-generator": "catalog:",
    "jsqr": "catalog:",
    "sharp": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "zxing-wasm": "catalog:"
  }
}
